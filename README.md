# Gym App 2 🏋️‍♂️

Sistema completo de gestión de gimnasio con Flask (backend) + Vite React (frontend).

## 🔧 Backend (Flask)
Ubicación: `/gym-app`

### Instalar entorno
```bash
cd gym-app
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
python run.py
